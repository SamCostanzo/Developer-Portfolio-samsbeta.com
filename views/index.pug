extends layout.pug

block content 
  article.grid-container.portfolio-intro
    div

      h1 Hi, i'm Sam.

      p.lead.text-light
        | Welcome to my developer portfolio! Below you can view all of my coding projects and see what technologies I used to build them with. Follow the link below my picture to read more about my developer journey and find my contact information.
  article.grid-container.portfolio-index
    
    .grid-x.grid-margin-x.small-up-2.medium-up-2.large-up-3

    
      each project in projects
        .cell
          a(href=`http://localhost:5000/project/${project.id}`)
            img.thumbnail(src= `${project.image_urls[0]}`)
            h5 #{project.project_name}
        

